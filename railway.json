{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "numReplicas": 1,
    "restartPolicyType": "ON_FAILURE",
    "startCommand": "python -m uvicorn backend.server:app --host 0.0.0.0 --port 8001",
    "healthcheckPath": "/api/",
    "healthcheckTimeout": 100
  }
}